# MultistreamViewer

<img width="1901" height="1344" alt="image" src="https://github.com/user-attachments/assets/a7f225e8-ab84-4141-9d22-52e8d929367f" />



This is a straightforward program intended for monitoring up to 4 streams at once.

Simply open the program and add your URLs to the appropriate boxes.

Each section is a Edge/Chrome browser window, so browser control keys will work (F5 to refresh, forwards/backwards buttons, etc).

The size, layout, and last visited URLs for each panel are saved, and will be restored when you relaunch the program.

This application is intended for managing things like chats/titles/thumbnails/settings/etc without having to open each site in a new browser window.

## Requirements

Requires .NET 8.0 runtime, which can be downloaded directly from microsoft. Note that you only need the runtime, not the SDK.

https://dotnet.microsoft.com/en-us/download/dotnet/8.0
